# 勘误审核工作台重做 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将卡牌勘误、我的勘误、审核、勘误包生成和管理员发布包审阅统一到一套可审计的卡牌工作台中。

**Architecture:** 后端以 `ErrataDraft` 作为每张卡唯一活动勘误副本，以 `ErrataAuditLog` 记录所有修改，以 `ErrataPackage` 作为唯一活动发布边界。前端抽出 `CardWorkbench` 复用当前卡牌浏览的树、三视图、JSON 编辑和渲染校验，不同页面只传入 `mode` 和权限配置。

**Tech Stack:** FastAPI、SQLAlchemy async、Alembic、PostgreSQL、pytest、React、TypeScript、Ant Design、Vite。

**Workspace Rule:** 按用户要求，直接在 `/Users/xziying/project/arkham/errata-platform` 当前仓库开发，不创建 git worktree。

---

## File Structure

### Backend

- Modify: `backend/app/models/user.py` — 将角色枚举改成 `勘误员`、`审核员`、`管理员`。
- Create: `backend/app/models/errata_draft.py` — 定义 `ErrataDraft`、`ErrataDraftStatus`、`ErrataAuditLog`、`ErrataAuditAction`、`ErrataPackage`、`ErrataPackageStatus`。
- Modify: `backend/app/models/__init__.py` — 导出新模型。
- Create: `backend/alembic/versions/20260426_errata_workbench.py` — 新增表、迁移用户角色、保留旧 `errata` 表数据。
- Modify: `backend/app/schemas/user.py` — 更新角色响应与用户管理请求。
- Create: `backend/app/schemas/errata_draft.py` — 定义工作台副本、日志、包响应模型。
- Modify: `backend/app/api/auth.py` — 增加三角色权限依赖和用户管理接口。
- Create: `backend/app/services/errata_drafts.py` — 封装创建/保存副本、审计日志、参与者查询、包锁定校验。
- Create: `backend/app/api/errata_drafts.py` — 新勘误副本 API。
- Modify: `backend/app/api/cards.py` — `tree` 增加 `scope`、参与者、状态、包字段。
- Modify: `backend/app/api/review.py` — 去掉驳回语义，改为生成唯一活动包。
- Create: `backend/app/api/packages.py` — 包列表、包详情、整包解锁、发布完成。
- Modify: `backend/app/api/publish.py` — 发布步骤改为以 `package_id` 读取后端包数据。
- Modify: `backend/app/main.py` — 注册 `errata_drafts` 和 `packages` 路由。
- Test: `backend/tests/test_roles_and_users.py` — 用户角色和用户管理测试。
- Test: `backend/tests/test_errata_drafts.py` — 唯一副本、多人日志、双面卡测试。
- Test: `backend/tests/test_packages.py` — 唯一活动包、整包解锁、发布完成测试。
- Modify: `backend/tests/test_review_fixes.py`、`backend/tests/test_publish_exports.py` — 调整旧审核/发布测试到新包模型。

### Frontend

- Modify: `frontend/src/types/index.ts` — 更新角色、卡牌树、draft、audit、package 类型。
- Modify: `frontend/src/hooks/useAuth.tsx` — 增加 `canErrata`、`canReview`、`canAdmin` 权限派生。
- Modify: `frontend/src/components/ProtectedRoute.tsx` — 增加 `ReviewerRoute` 和权限判断。
- Modify: `frontend/src/components/AppLayout.tsx` — 更新导航和路由。
- Create: `frontend/src/api/errataDrafts.ts` — 勘误副本和日志 API。
- Create: `frontend/src/api/packages.ts` — 勘误包 API。
- Modify: `frontend/src/api/auth.ts`、`frontend/src/api/cards.ts`、`frontend/src/api/admin.ts` — 对齐新接口。
- Create: `frontend/src/components/workbench/CardWorkbench.tsx` — 工作台容器。
- Create: `frontend/src/components/workbench/CardTreePanel.tsx` — 左侧树与状态/参与者展示。
- Create: `frontend/src/components/workbench/CardDetailPanel.tsx` — 详情、三视图、编辑器组合。
- Create: `frontend/src/components/workbench/ErrataAuditTimeline.tsx` — 审计日志时间线。
- Create: `frontend/src/components/workbench/PackageActionBar.tsx` — 审核页生成包、发布包解锁动作。
- Modify: `frontend/src/pages/CardBrowserPage.tsx` — 改为 `CardWorkbench mode="errata-all"`。
- Create: `frontend/src/pages/MyErrataPage.tsx` — `CardWorkbench mode="my-errata"`。
- Modify: `frontend/src/pages/ReviewPage.tsx` — `CardWorkbench mode="review"`。
- Modify: `frontend/src/pages/PublishPage.tsx` — 包列表、包审阅、选包进入现有发布步骤。
- Create: `frontend/src/pages/UserManagementPage.tsx` — 管理员用户管理。

---

## Task 1: Backend Roles and User Management

**Files:**
- Modify: `backend/app/models/user.py`
- Modify: `backend/app/schemas/user.py`
- Modify: `backend/app/api/auth.py`
- Create: `backend/tests/test_roles_and_users.py`

- [ ] **Step 1: Write role and user management tests**

Create `backend/tests/test_roles_and_users.py`:

```python
import pytest
from httpx import AsyncClient
from sqlalchemy.ext.asyncio import AsyncSession

from app.models.user import User, UserRole
from app.utils.security import hash_password


@pytest.mark.asyncio
async def test_admin_can_create_reviewer(client: AsyncClient, db_session: AsyncSession):
    admin = User(username="admin-role-test", password_hash=hash_password("pw"), role=UserRole.ADMIN)
    db_session.add(admin)
    await db_session.commit()

    login = await client.post("/api/auth/login", json={"username": "admin-role-test", "password": "pw"})
    token = login.json()["token"]

    response = await client.post(
        "/api/auth/users",
        headers={"Authorization": f"Bearer {token}"},
        json={"username": "reviewer-a", "password": "pw2", "role": "审核员"},
    )

    assert response.status_code == 200
    assert response.json()["username"] == "reviewer-a"
    assert response.json()["role"] == "审核员"


@pytest.mark.asyncio
async def test_errata_user_cannot_list_users(client: AsyncClient, db_session: AsyncSession):
    user = User(username="errata-role-test", password_hash=hash_password("pw"), role=UserRole.ERRATA)
    db_session.add(user)
    await db_session.commit()

    login = await client.post("/api/auth/login", json={"username": "errata-role-test", "password": "pw"})
    token = login.json()["token"]

    response = await client.get("/api/auth/users", headers={"Authorization": f"Bearer {token}"})

    assert response.status_code == 403
    assert response.json()["detail"] == "需要管理员权限"


@pytest.mark.asyncio
async def test_admin_can_disable_and_reset_password(client: AsyncClient, db_session: AsyncSession):
    admin = User(username="admin-user-mgmt", password_hash=hash_password("pw"), role=UserRole.ADMIN)
    target = User(username="target-user", password_hash=hash_password("old"), role=UserRole.ERRATA)
    db_session.add_all([admin, target])
    await db_session.commit()
    await db_session.refresh(target)

    login = await client.post("/api/auth/login", json={"username": "admin-user-mgmt", "password": "pw"})
    token = login.json()["token"]

    patch = await client.patch(
        f"/api/auth/users/{target.id}",
        headers={"Authorization": f"Bearer {token}"},
        json={"role": "审核员", "is_active": False},
    )
    assert patch.status_code == 200
    assert patch.json()["role"] == "审核员"
    assert patch.json()["is_active"] is False

    reset = await client.post(
        f"/api/auth/users/{target.id}/reset-password",
        headers={"Authorization": f"Bearer {token}"},
        json={"password": "new-password"},
    )
    assert reset.status_code == 200
    assert reset.json()["ok"] is True
```

- [ ] **Step 2: Run role tests and verify they fail**

Run:

```bash
cd /Users/xziying/project/arkham/errata-platform/backend
PYTHONPATH=. ./.venv/bin/python -m pytest tests/test_roles_and_users.py -q
```

Expected: failures mention `UserRole.ERRATA` or missing `PATCH /api/auth/users/{id}`.

- [ ] **Step 3: Update user role model**

Modify `backend/app/models/user.py`:

```python
class UserRole(str, enum.Enum):
    ERRATA = "勘误员"
    REVIEWER = "审核员"
    ADMIN = "管理员"
```

Keep `User.role` as `SAEnum(UserRole)` and change the default to `UserRole.ERRATA`.

- [ ] **Step 4: Update user schemas**

Modify `backend/app/schemas/user.py` so `CreateUserRequest.role` accepts the three role strings, and add:

```python
class UpdateUserRequest(BaseModel):
    role: str | None = None
    is_active: bool | None = None


class ResetPasswordRequest(BaseModel):
    password: str
```

- [ ] **Step 5: Update auth API permissions and user management**

Modify `backend/app/api/auth.py`:

```python
async def require_reviewer(current_user: User = Depends(require_user)) -> User:
    if current_user.role not in {UserRole.REVIEWER, UserRole.ADMIN}:
        raise HTTPException(status_code=403, detail="需要审核权限")
    return current_user


async def require_errata_user(current_user: User = Depends(require_user)) -> User:
    if current_user.role not in {UserRole.ERRATA, UserRole.ADMIN}:
        raise HTTPException(status_code=403, detail="需要勘误权限")
    return current_user


def parse_user_role(role: str) -> UserRole:
    try:
        return UserRole(role)
    except ValueError as exc:
        raise HTTPException(status_code=400, detail="无效的用户角色") from exc
```

Update `create_user()` to call `parse_user_role(req.role)`. Add endpoints:

```python
@router.patch("/users/{user_id}", response_model=UserResponse)
async def update_user(
    user_id: int,
    req: UpdateUserRequest,
    db: AsyncSession = Depends(get_db),
    admin: User = Depends(require_admin),
):
    user = await db.get(User, user_id)
    if user is None:
        raise HTTPException(status_code=404, detail="用户不存在")
    if req.role is not None:
        user.role = parse_user_role(req.role)
    if req.is_active is not None:
        user.is_active = req.is_active
    await db.commit()
    await db.refresh(user)
    return user


@router.post("/users/{user_id}/reset-password")
async def reset_user_password(
    user_id: int,
    req: ResetPasswordRequest,
    db: AsyncSession = Depends(get_db),
    admin: User = Depends(require_admin),
):
    user = await db.get(User, user_id)
    if user is None:
        raise HTTPException(status_code=404, detail="用户不存在")
    user.password_hash = hash_password(req.password)
    await db.commit()
    return {"ok": True}
```

- [ ] **Step 6: Run role tests and commit**

Run:

```bash
cd /Users/xziying/project/arkham/errata-platform/backend
PYTHONPATH=. ./.venv/bin/python -m pytest tests/test_roles_and_users.py tests/test_auth.py -q
```

Expected: all selected tests pass.

Commit:

```bash
git add backend/app/models/user.py backend/app/schemas/user.py backend/app/api/auth.py backend/tests/test_roles_and_users.py
git commit -m "重构用户角色与管理接口"
```

---

## Task 2: Errata Draft Models and Migration

**Files:**
- Create: `backend/app/models/errata_draft.py`
- Modify: `backend/app/models/__init__.py`
- Create: `backend/alembic/versions/20260426_errata_workbench.py`
- Create: `backend/tests/test_models_errata_workbench.py`

- [ ] **Step 1: Write model tests**

Create `backend/tests/test_models_errata_workbench.py`:

```python
import pytest
from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

from app.models.errata_draft import ErrataDraft, ErrataDraftStatus, ErrataPackage, ErrataPackageStatus


@pytest.mark.asyncio
async def test_create_errata_draft_with_faces(db_session: AsyncSession):
    draft = ErrataDraft(
        arkhamdb_id="01001",
        status=ErrataDraftStatus.ERRATA,
        original_faces={"a": {"name": "旧正面"}, "b": {"name": "旧背面"}},
        modified_faces={"a": {"name": "新正面"}, "b": {"name": "旧背面"}},
        changed_faces=["a"],
        created_by=1,
        updated_by=1,
    )
    db_session.add(draft)
    await db_session.commit()

    result = await db_session.execute(select(ErrataDraft).where(ErrataDraft.arkhamdb_id == "01001"))
    saved = result.scalar_one()

    assert saved.status == ErrataDraftStatus.ERRATA
    assert saved.modified_faces["a"]["name"] == "新正面"
    assert saved.changed_faces == ["a"]


@pytest.mark.asyncio
async def test_create_waiting_publish_package(db_session: AsyncSession):
    package = ErrataPackage(package_no="ERRATA-20260426-001", status=ErrataPackageStatus.WAITING_PUBLISH, created_by=1)
    db_session.add(package)
    await db_session.commit()

    result = await db_session.execute(select(ErrataPackage).where(ErrataPackage.package_no == "ERRATA-20260426-001"))
    saved = result.scalar_one()

    assert saved.status == ErrataPackageStatus.WAITING_PUBLISH
```

- [ ] **Step 2: Run model tests and verify they fail**

Run:

```bash
cd /Users/xziying/project/arkham/errata-platform/backend
PYTHONPATH=. ./.venv/bin/python -m pytest tests/test_models_errata_workbench.py -q
```

Expected: import failure for `app.models.errata_draft`.

- [ ] **Step 3: Create errata draft models**

Create `backend/app/models/errata_draft.py`:

```python
import enum
from datetime import datetime
from sqlalchemy import DateTime, Enum as SAEnum, ForeignKey, Integer, JSON, String, Text, func
from sqlalchemy.orm import Mapped, mapped_column

from app.database import Base


class ErrataDraftStatus(str, enum.Enum):
    ERRATA = "勘误"
    WAITING_PUBLISH = "待发布"
    ARCHIVED = "已归档"


class ErrataAuditAction(str, enum.Enum):
    CREATE = "创建副本"
    SAVE = "保存勘误"
    REVIEW_SAVE = "审核修改"
    PACKAGE = "生成包"
    UNLOCK = "解锁退回"
    PUBLISH = "发布完成"


class ErrataPackageStatus(str, enum.Enum):
    WAITING_PUBLISH = "待发布"
    PUBLISHING = "发布中"
    PUBLISHED = "已发布"
    UNLOCKED = "已退回"


class ErrataDraft(Base):
    __tablename__ = "errata_drafts"

    id: Mapped[int] = mapped_column(primary_key=True, autoincrement=True)
    arkhamdb_id: Mapped[str] = mapped_column(String(16), nullable=False, index=True)
    status: Mapped[ErrataDraftStatus] = mapped_column(SAEnum(ErrataDraftStatus), default=ErrataDraftStatus.ERRATA, nullable=False, index=True)
    original_faces: Mapped[dict] = mapped_column(JSON, nullable=False, default=dict)
    modified_faces: Mapped[dict] = mapped_column(JSON, nullable=False, default=dict)
    changed_faces: Mapped[list] = mapped_column(JSON, nullable=False, default=list)
    rendered_previews: Mapped[dict] = mapped_column(JSON, nullable=False, default=dict)
    package_id: Mapped[int | None] = mapped_column(Integer, ForeignKey("errata_packages.id"), nullable=True, index=True)
    created_by: Mapped[int] = mapped_column(Integer, ForeignKey("users.id"), nullable=False)
    updated_by: Mapped[int] = mapped_column(Integer, ForeignKey("users.id"), nullable=False)
    archived_at: Mapped[datetime | None] = mapped_column(DateTime, nullable=True, index=True)
    created_at: Mapped[datetime] = mapped_column(DateTime, server_default=func.now())
    updated_at: Mapped[datetime] = mapped_column(DateTime, server_default=func.now(), onupdate=func.now())


class ErrataAuditLog(Base):
    __tablename__ = "errata_audit_logs"

    id: Mapped[int] = mapped_column(primary_key=True, autoincrement=True)
    draft_id: Mapped[int] = mapped_column(Integer, ForeignKey("errata_drafts.id"), nullable=False, index=True)
    arkhamdb_id: Mapped[str] = mapped_column(String(16), nullable=False, index=True)
    user_id: Mapped[int] = mapped_column(Integer, ForeignKey("users.id"), nullable=False, index=True)
    action: Mapped[ErrataAuditAction] = mapped_column(SAEnum(ErrataAuditAction), nullable=False, index=True)
    from_status: Mapped[str | None] = mapped_column(String(32), nullable=True)
    to_status: Mapped[str | None] = mapped_column(String(32), nullable=True)
    changed_faces: Mapped[list] = mapped_column(JSON, nullable=False, default=list)
    content_hash: Mapped[str | None] = mapped_column(String(64), nullable=True)
    diff_summary: Mapped[str | None] = mapped_column(Text, nullable=True)
    created_at: Mapped[datetime] = mapped_column(DateTime, server_default=func.now())


class ErrataPackage(Base):
    __tablename__ = "errata_packages"

    id: Mapped[int] = mapped_column(primary_key=True, autoincrement=True)
    package_no: Mapped[str] = mapped_column(String(32), unique=True, nullable=False, index=True)
    status: Mapped[ErrataPackageStatus] = mapped_column(SAEnum(ErrataPackageStatus), nullable=False, index=True)
    created_by: Mapped[int] = mapped_column(Integer, ForeignKey("users.id"), nullable=False)
    published_by: Mapped[int | None] = mapped_column(Integer, ForeignKey("users.id"), nullable=True)
    unlocked_by: Mapped[int | None] = mapped_column(Integer, ForeignKey("users.id"), nullable=True)
    note: Mapped[str | None] = mapped_column(Text, nullable=True)
    created_at: Mapped[datetime] = mapped_column(DateTime, server_default=func.now())
    updated_at: Mapped[datetime] = mapped_column(DateTime, server_default=func.now(), onupdate=func.now())
    published_at: Mapped[datetime | None] = mapped_column(DateTime, nullable=True)
```

- [ ] **Step 4: Export models**

Modify `backend/app/models/__init__.py` to import and export the new models and enums.

- [ ] **Step 5: Create Alembic migration**

Create `backend/alembic/versions/20260426_errata_workbench.py` with table creation for `errata_drafts`, `errata_audit_logs`, `errata_packages`, plus SQL updates mapping existing role value `用户` to `勘误员`. Use the existing `b8888de7362f` revision as `down_revision`.

- [ ] **Step 6: Run model tests and commit**

Run:

```bash
cd /Users/xziying/project/arkham/errata-platform/backend
PYTHONPATH=. ./.venv/bin/python -m pytest tests/test_models.py tests/test_models_errata_workbench.py -q
```

Expected: all selected tests pass.

Commit:

```bash
git add backend/app/models/errata_draft.py backend/app/models/__init__.py backend/alembic/versions/20260426_errata_workbench.py backend/tests/test_models_errata_workbench.py
git commit -m "添加勘误副本与包模型"
```

---

## Task 3: Errata Draft Service and API

**Files:**
- Create: `backend/app/schemas/errata_draft.py`
- Create: `backend/app/services/errata_drafts.py`
- Create: `backend/app/api/errata_drafts.py`
- Modify: `backend/app/main.py`
- Create: `backend/tests/test_errata_drafts.py`

- [ ] **Step 1: Write draft API tests**

Create `backend/tests/test_errata_drafts.py` with tests for saving the same `arkhamdb_id` by two users and verifying one draft plus two participant logs. Use fixtures from `backend/tests/conftest.py` and create `CardIndex`/`LocalCardFile` rows for `01001` before calling the API.

- [ ] **Step 2: Run draft tests and verify they fail**

Run:

```bash
cd /Users/xziying/project/arkham/errata-platform/backend
PYTHONPATH=. ./.venv/bin/python -m pytest tests/test_errata_drafts.py -q
```

Expected: missing `/api/errata-drafts/{arkhamdb_id}` routes.

- [ ] **Step 3: Create draft schemas**

Create `backend/app/schemas/errata_draft.py` with request and response classes:

```python
from datetime import datetime
from pydantic import BaseModel


class SaveErrataDraftRequest(BaseModel):
    modified_faces: dict[str, dict]
    changed_faces: list[str]
    diff_summary: str | None = None


class ErrataDraftResponse(BaseModel):
    id: int
    arkhamdb_id: str
    status: str
    original_faces: dict[str, dict]
    modified_faces: dict[str, dict]
    changed_faces: list[str]
    rendered_previews: dict[str, str | None]
    package_id: int | None
    participant_usernames: list[str]
    created_at: datetime
    updated_at: datetime


class ErrataAuditLogResponse(BaseModel):
    id: int
    arkhamdb_id: str
    username: str
    action: str
    from_status: str | None
    to_status: str | None
    changed_faces: list[str]
    diff_summary: str | None
    created_at: datetime
```

- [ ] **Step 4: Implement service functions**

Create `backend/app/services/errata_drafts.py` with functions:

- `get_active_draft(db, arkhamdb_id)`
- `load_original_faces(db, arkhamdb_id)` reads all `LocalCardFile` rows for the card and returns `{face: content}`.
- `save_draft(db, arkhamdb_id, body, user, action)` creates or updates one active draft.
- `append_audit_log(db, draft, user, action, from_status, to_status, changed_faces, diff_summary)`.
- `get_participant_usernames(db, arkhamdb_id)` joins `ErrataAuditLog` and `User`.
- `ensure_draft_editable(draft)` raises `HTTPException(409, "该卡牌已进入待发布包，请管理员解锁后再修改")` for `待发布`.

- [ ] **Step 5: Implement API routes**

Create `backend/app/api/errata_drafts.py`:

```python
from fastapi import APIRouter, Depends, HTTPException
from sqlalchemy.ext.asyncio import AsyncSession

from app.api.auth import require_user
from app.database import get_db
from app.models.errata_draft import ErrataAuditAction
from app.models.user import User, UserRole
from app.schemas.errata_draft import ErrataDraftResponse, ErrataAuditLogResponse, SaveErrataDraftRequest
from app.services.errata_drafts import get_active_draft, get_logs, get_participant_usernames, save_draft

router = APIRouter(prefix="/api/errata-drafts", tags=["勘误副本"])


def can_save(user: User) -> bool:
    return user.role in {UserRole.ERRATA, UserRole.REVIEWER, UserRole.ADMIN}


@router.get("/{arkhamdb_id}", response_model=ErrataDraftResponse)
async def get_draft(arkhamdb_id: str, db: AsyncSession = Depends(get_db), current_user: User = Depends(require_user)):
    draft = await get_active_draft(db, arkhamdb_id)
    if draft is None:
        raise HTTPException(status_code=404, detail="该卡牌还没有勘误副本")
    participants = await get_participant_usernames(db, arkhamdb_id)
    return ErrataDraftResponse(
        id=draft.id,
        arkhamdb_id=draft.arkhamdb_id,
        status=draft.status.value,
        original_faces=draft.original_faces,
        modified_faces=draft.modified_faces,
        changed_faces=draft.changed_faces,
        rendered_previews=draft.rendered_previews,
        package_id=draft.package_id,
        participant_usernames=participants,
        created_at=draft.created_at,
        updated_at=draft.updated_at,
    )


@router.put("/{arkhamdb_id}", response_model=ErrataDraftResponse)
async def put_draft(arkhamdb_id: str, body: SaveErrataDraftRequest, db: AsyncSession = Depends(get_db), current_user: User = Depends(require_user)):
    if not can_save(current_user):
        raise HTTPException(status_code=403, detail="需要勘误或审核权限")
    action = ErrataAuditAction.REVIEW_SAVE if current_user.role == UserRole.REVIEWER else ErrataAuditAction.SAVE
    draft = await save_draft(db, arkhamdb_id, body, current_user, action)
    participants = await get_participant_usernames(db, arkhamdb_id)
    return ErrataDraftResponse(
        id=draft.id,
        arkhamdb_id=draft.arkhamdb_id,
        status=draft.status.value,
        original_faces=draft.original_faces,
        modified_faces=draft.modified_faces,
        changed_faces=draft.changed_faces,
        rendered_previews=draft.rendered_previews,
        package_id=draft.package_id,
        participant_usernames=participants,
        created_at=draft.created_at,
        updated_at=draft.updated_at,
    )
```

Also implement `GET /{arkhamdb_id}/logs` using `get_logs()`.

- [ ] **Step 6: Register route and run tests**

Modify `backend/app/main.py` to include `errata_drafts.router`.

Run:

```bash
cd /Users/xziying/project/arkham/errata-platform/backend
PYTHONPATH=. ./.venv/bin/python -m pytest tests/test_errata_drafts.py -q
```

Expected: draft tests pass.

Commit:

```bash
git add backend/app/schemas/errata_draft.py backend/app/services/errata_drafts.py backend/app/api/errata_drafts.py backend/app/main.py backend/tests/test_errata_drafts.py
git commit -m "添加勘误副本接口"
```

---

## Task 4: Card Tree Scopes and Review Package API

**Files:**
- Modify: `backend/app/api/cards.py`
- Modify: `backend/app/api/review.py`
- Create: `backend/app/api/packages.py`
- Modify: `backend/app/main.py`
- Create: `backend/tests/test_packages.py`

- [ ] **Step 1: Write package tests**

Create `backend/tests/test_packages.py` covering:

- reviewer can create a package from two `勘误` drafts.
- second package creation fails while the first package is `待发布`.
- admin unlocks package and all package drafts return to `勘误`.

- [ ] **Step 2: Run package tests and verify they fail**

Run:

```bash
cd /Users/xziying/project/arkham/errata-platform/backend
PYTHONPATH=. ./.venv/bin/python -m pytest tests/test_packages.py -q
```

Expected: missing package routes.

- [ ] **Step 3: Update `cards/tree` response**

Modify `backend/app/api/cards.py`:

- Add `scope: str = "all"` and `package_id: int | None = None` query params.
- For each card, compute `errata_state` from active `ErrataDraft`: no draft = `正常`, draft status `勘误` = `勘误`, draft status `待发布` = `待发布`.
- Add `participant_usernames`, `package_id`, `latest_audit_at` fields.
- Apply scope filters:
  - `all`: all local cards.
  - `mine`: cards with an audit log by `current_user.id`.
  - `review`: active draft status `勘误`.
  - `package`: active draft `package_id` matches query.

- [ ] **Step 4: Replace review approve/reject with package creation**

Modify `backend/app/api/review.py`:

- Keep `GET /pending` as compatibility wrapper for active `勘误` drafts.
- Remove use of `ErrataStatus.REJECTED` from new flow.
- Add `POST /api/admin/review/package` requiring `require_reviewer`.
- Validate no package with status `待发布` or `发布中` exists.
- Create `ErrataPackage` and set selected drafts to `待发布`.
- Write `生成包` audit logs.

- [ ] **Step 5: Add packages API**

Create `backend/app/api/packages.py`:

- `GET /api/admin/packages` returns packages ordered by `created_at desc`.
- `GET /api/admin/packages/{package_id}` returns package and drafts.
- `POST /api/admin/packages/{package_id}/unlock` requires admin, changes package to `已退回`, clears draft `package_id`, sets draft status `勘误`, writes `解锁退回` logs.
- `POST /api/admin/packages/{package_id}/complete` marks package `已发布`, marks drafts `已归档`, sets `archived_at`, writes `发布完成` logs.

- [ ] **Step 6: Register package route and run tests**

Modify `backend/app/main.py` to include `packages.router`.

Run:

```bash
cd /Users/xziying/project/arkham/errata-platform/backend
PYTHONPATH=. ./.venv/bin/python -m pytest tests/test_packages.py tests/test_review_fixes.py -q
```

Expected: selected tests pass after updating old review assertions to package semantics.

Commit:

```bash
git add backend/app/api/cards.py backend/app/api/review.py backend/app/api/packages.py backend/app/main.py backend/tests/test_packages.py backend/tests/test_review_fixes.py
git commit -m "实现勘误包生成与解锁"
```

---

## Task 5: Package-Based Publishing

**Files:**
- Modify: `backend/app/api/publish.py`
- Modify: `backend/tests/test_publish_exports.py`

- [ ] **Step 1: Write publishing tests for package input**

Modify `backend/tests/test_publish_exports.py` to assert:

- `step1-generate-sheets` accepts `package_id`.
- if package does not exist, returns `404` with `勘误包不存在`.
- if package is not `待发布`, returns `409`.
- backend reads package drafts instead of trusting frontend `approved_cards`.

- [ ] **Step 2: Run publishing tests and verify they fail**

Run:

```bash
cd /Users/xziying/project/arkham/errata-platform/backend
PYTHONPATH=. ./.venv/bin/python -m pytest tests/test_publish_exports.py -q
```

Expected: failures mention old body shape or missing package validation.

- [ ] **Step 3: Refactor publish helpers**

Modify `backend/app/api/publish.py`:

- Add helper `load_publish_package(db, package_id)`.
- Add helper `build_approved_cards_from_package(db, package)` that reads `ErrataDraft.modified_faces` and mapping index.
- Change step1 body to require `package_id`.
- Change step3 body to require `package_id`, `sheet_urls`, `sheet_grids`.
- Keep user-provided upload config for image host, but not card content.

- [ ] **Step 4: Run publishing tests and commit**

Run:

```bash
cd /Users/xziying/project/arkham/errata-platform/backend
PYTHONPATH=. ./.venv/bin/python -m pytest tests/test_publish_exports.py tests/test_packages.py -q
```

Expected: selected tests pass.

Commit:

```bash
git add backend/app/api/publish.py backend/tests/test_publish_exports.py
git commit -m "按勘误包驱动发布流程"
```

---

## Task 6: Frontend Types, Auth, and API Clients

**Files:**
- Modify: `frontend/src/types/index.ts`
- Modify: `frontend/src/hooks/useAuth.tsx`
- Modify: `frontend/src/components/ProtectedRoute.tsx`
- Modify: `frontend/src/api/auth.ts`
- Modify: `frontend/src/api/cards.ts`
- Create: `frontend/src/api/errataDrafts.ts`
- Create: `frontend/src/api/packages.ts`
- Modify: `frontend/src/api/admin.ts`

- [ ] **Step 1: Update shared types**

Modify `frontend/src/types/index.ts`:

```ts
export type UserRole = '勘误员' | '审核员' | '管理员';
export type CardErrataState = '正常' | '勘误' | '待发布';
export type WorkbenchMode = 'errata-all' | 'my-errata' | 'review' | 'package-review';

export interface User {
  id: number;
  username: string;
  role: UserRole;
  is_active: boolean;
}

export interface ErrataDraft {
  id: number;
  arkhamdb_id: string;
  status: '勘误' | '待发布' | '已归档';
  original_faces: Record<string, Record<string, unknown>>;
  modified_faces: Record<string, Record<string, unknown>>;
  changed_faces: string[];
  rendered_previews: Record<string, string | null>;
  package_id: number | null;
  participant_usernames: string[];
  created_at: string;
  updated_at: string;
}

export interface ErrataAuditLog {
  id: number;
  arkhamdb_id: string;
  username: string;
  action: string;
  from_status: string | null;
  to_status: string | null;
  changed_faces: string[];
  diff_summary: string | null;
  created_at: string;
}

export interface ErrataPackage {
  id: number;
  package_no: string;
  status: '待发布' | '发布中' | '已发布' | '已退回';
  note: string | null;
  created_at: string;
  updated_at: string;
  published_at: string | null;
}
```

Extend `CardTreeCard` with `participant_usernames: string[]`, `package_id: number | null`, and `latest_audit_at: string | null`.

- [ ] **Step 2: Update auth hook permissions**

Modify `frontend/src/hooks/useAuth.tsx` context fields:

```ts
canErrata: boolean;
canReview: boolean;
canAdmin: boolean;
```

Return:

```ts
const canAdmin = user?.role === '管理员';
const canErrata = user?.role === '勘误员' || canAdmin;
const canReview = user?.role === '审核员' || canAdmin;
```

- [ ] **Step 3: Update protected routes**

Modify `frontend/src/components/ProtectedRoute.tsx` to export:

```tsx
export function ReviewerRoute({ children }: { children: React.ReactNode }) {
  const { token, loading, canReview } = useAuth();
  if (loading) return <Spin />;
  if (!token) return <Navigate to="/login" replace />;
  if (!canReview) return <Navigate to="/cards" replace />;
  return <>{children}</>;
}
```

Keep `AdminRoute` based on `canAdmin`.

- [ ] **Step 4: Add API clients**

Create `frontend/src/api/errataDrafts.ts` with `fetchErrataDraft`, `saveErrataDraft`, `fetchErrataDraftLogs`.

Create `frontend/src/api/packages.ts` with `fetchPackages`, `fetchPackageDetail`, `unlockPackage`, `completePackage`.

Modify `frontend/src/api/cards.ts` so `fetchCardTree` accepts `{ keyword, scope, package_id }`.

- [ ] **Step 5: Build frontend and commit**

Run:

```bash
cd /Users/xziying/project/arkham/errata-platform/frontend
npm run build -- --mode development
```

Expected: TypeScript build succeeds.

Commit:

```bash
git add frontend/src/types/index.ts frontend/src/hooks/useAuth.tsx frontend/src/components/ProtectedRoute.tsx frontend/src/api/auth.ts frontend/src/api/cards.ts frontend/src/api/errataDrafts.ts frontend/src/api/packages.ts frontend/src/api/admin.ts
git commit -m "更新前端权限与勘误包接口"
```

---

## Task 7: Shared Card Workbench Components

**Files:**
- Create: `frontend/src/components/workbench/CardWorkbench.tsx`
- Create: `frontend/src/components/workbench/CardTreePanel.tsx`
- Create: `frontend/src/components/workbench/CardDetailPanel.tsx`
- Create: `frontend/src/components/workbench/ErrataAuditTimeline.tsx`
- Create: `frontend/src/components/workbench/PackageActionBar.tsx`
- Modify: `frontend/src/pages/CardBrowserPage.tsx`
- Create: `frontend/src/pages/MyErrataPage.tsx`
- Modify: `frontend/src/pages/ReviewPage.tsx`
- Modify: `frontend/src/components/AppLayout.tsx`

- [ ] **Step 1: Extract tree panel**

Move tree helpers from `CardBrowserPage.tsx` into `frontend/src/components/workbench/CardTreePanel.tsx`:

- `collectLeafKeys`
- `collectExpandableKeys`
- `summarizeTree`
- status tag rendering
- participant rendering
- group title rendering
- card title rendering
- `toTreeData`
- `findCard`

Expose props:

```ts
interface CardTreePanelProps {
  tree: CardTreeNode[];
  loading: boolean;
  keyword: string;
  selectedKey: string | null;
  expandedKeys: Key[];
  onKeywordChange: (value: string) => void;
  onSelectedKeyChange: (key: string) => void;
  onExpandedKeysChange: (keys: Key[]) => void;
}
```

- [ ] **Step 2: Extract detail panel**

Create `frontend/src/components/workbench/CardDetailPanel.tsx` using existing `CardComparison` and `JsonEditor`. It receives current `CardDetail`, `ErrataDraft | null`, preview items, logs, editable flag, and callbacks for save/render.

- [ ] **Step 3: Add audit timeline**

Create `frontend/src/components/workbench/ErrataAuditTimeline.tsx` using Ant Design `Timeline`. Display `username`、`action`、`changed_faces`、`diff_summary`、`created_at`.

- [ ] **Step 4: Add package action bar**

Create `frontend/src/components/workbench/PackageActionBar.tsx`:

- `review` mode: button `生成勘误包` enabled when selected cards exist.
- `package-review` mode: button `解锁整包` for admin package review.

- [ ] **Step 5: Create CardWorkbench container**

Create `frontend/src/components/workbench/CardWorkbench.tsx`:

- maps mode to `scope`: `errata-all -> all`, `my-errata -> mine`, `review -> review`, `package-review -> package`.
- loads card tree with `fetchCardTree`.
- loads card detail, current draft if exists, logs if exists, and preview images.
- saves via `saveErrataDraft`.
- renders via existing preview endpoint and refreshes cache bust.
- shows non-fatal warnings when preview is slow, preserving current card detail display.

- [ ] **Step 6: Replace pages with workbench wrappers**

Modify pages:

```tsx
export default function CardBrowserPage() {
  return <CardWorkbench mode="errata-all" />;
}
```

Create `MyErrataPage.tsx`:

```tsx
import CardWorkbench from '../components/workbench/CardWorkbench';

export default function MyErrataPage() {
  return <CardWorkbench mode="my-errata" />;
}
```

Modify `ReviewPage.tsx`:

```tsx
import CardWorkbench from '../components/workbench/CardWorkbench';

export default function ReviewPage() {
  return <CardWorkbench mode="review" />;
}
```

- [ ] **Step 7: Update routes and build**

Modify `frontend/src/components/AppLayout.tsx`:

- `/cards`: `ProtectedRoute` + `CardBrowserPage`
- `/my-errata`: `ProtectedRoute` + `MyErrataPage`
- `/admin/review`: `ReviewerRoute` + `ReviewPage`
- add `/admin/users`: `AdminRoute` + `UserManagementPage` after Task 8

Run:

```bash
cd /Users/xziying/project/arkham/errata-platform/frontend
npm run build -- --mode development
```

Expected: TypeScript build succeeds.

Commit:

```bash
git add frontend/src/components/workbench frontend/src/pages/CardBrowserPage.tsx frontend/src/pages/MyErrataPage.tsx frontend/src/pages/ReviewPage.tsx frontend/src/components/AppLayout.tsx
git commit -m "抽取共享卡牌工作台"
```

---

## Task 8: Admin User Management and Publish Package UI

**Files:**
- Create: `frontend/src/pages/UserManagementPage.tsx`
- Modify: `frontend/src/pages/PublishPage.tsx`
- Modify: `frontend/src/components/AppLayout.tsx`

- [ ] **Step 1: Implement user management page**

Create `frontend/src/pages/UserManagementPage.tsx`:

- Table columns: username, role, active status, actions.
- Create modal: username, password, role.
- Edit action: role select, active switch.
- Reset password action: password input.

Use `fetchUsers`, `createUser`, `updateUser`, `resetPassword` from `frontend/src/api/auth.ts`.

- [ ] **Step 2: Add user management route**

Modify `frontend/src/components/AppLayout.tsx` navigation and route:

```tsx
<Route path="/admin/users" element={<AdminRoute><UserManagementPage /></AdminRoute>} />
```

Add menu item `用户管理` only when `canAdmin`.

- [ ] **Step 3: Refactor PublishPage to package list first**

Modify `frontend/src/pages/PublishPage.tsx`:

- top area lists packages from `fetchPackages()`.
- selecting a `待发布` package sets `packageId`.
- existing step buttons use `package_id` instead of free-form `batchId`.
- package detail panel uses `CardWorkbench mode="package-review" packageId={selectedPackageId}`.
- add `解锁整包` button for `待发布` package.

- [ ] **Step 4: Build frontend and commit**

Run:

```bash
cd /Users/xziying/project/arkham/errata-platform/frontend
npm run build -- --mode development
```

Expected: TypeScript build succeeds.

Commit:

```bash
git add frontend/src/pages/UserManagementPage.tsx frontend/src/pages/PublishPage.tsx frontend/src/components/AppLayout.tsx
git commit -m "添加用户管理与包发布入口"
```

---

## Task 9: Full Verification and App Smoke Test

**Files:**
- Modify test files only if verification exposes mismatches in intended behavior.

- [ ] **Step 1: Run backend tests**

Run:

```bash
cd /Users/xziying/project/arkham/errata-platform/backend
PYTHONPATH=. ./.venv/bin/python -m pytest -q
```

Expected: all backend tests pass.

- [ ] **Step 2: Run frontend build**

Run:

```bash
cd /Users/xziying/project/arkham/errata-platform/frontend
npm run build -- --mode development
```

Expected: build succeeds. Vite chunk size warning is acceptable if there are no TypeScript errors.

- [ ] **Step 3: Start backend**

Run:

```bash
cd /Users/xziying/project/arkham/errata-platform/backend
PYTHONPATH=. ./.venv/bin/python -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

Expected: server listens on `http://127.0.0.1:8000`.

- [ ] **Step 4: Start frontend**

Run in another terminal:

```bash
cd /Users/xziying/project/arkham/errata-platform/frontend
npm run dev -- --host 127.0.0.1
```

Expected: Vite prints a local URL such as `http://127.0.0.1:5173/`.

- [ ] **Step 5: Manual browser checks**

Use the running app and verify:

1. Admin login works with existing admin account.
2. Admin can create `勘误员` and `审核员` accounts.
3. 勘误员 can open `/cards`, edit one card, save, and see state `勘误`.
4. Another 勘误员 editing the same card updates the same draft and shows both usernames.
5. 审核员 opens `/admin/review`, sees the same three-view workbench, edits, renders, and generates a package.
6. `/admin/publish` shows the package and blocks creation of a second active package.
7. Admin can unlock the package and the card returns to `勘误`.

- [ ] **Step 6: Final commit if smoke fixes were needed**

If smoke testing required code changes, commit them:

```bash
git add backend frontend
git commit -m "修复勘误工作台联调问题"
```

If no code changes were needed, do not create an empty commit.

---

## Self-Review

### Spec Coverage

- 统一工作台：Task 7 implements `CardWorkbench` and page wrappers.
- 三角色权限：Task 1 and Task 6 implement backend and frontend permissions.
- 单一活动副本：Task 2 and Task 3 implement `ErrataDraft` and save semantics.
- 审计日志和参与者显示：Task 3 backend logs, Task 4 tree response, Task 7 UI timeline and participants.
- 审核员直接修改：Task 3 allows reviewer save, Task 7 review mode exposes editor.
- 唯一活动勘误包：Task 4 enforces only one waiting/publishing package.
- 整包解锁：Task 4 backend unlock, Task 8 publish UI action.
- 发布按包读取数据：Task 5 replaces frontend-provided cards with `package_id`.
- 用户管理：Task 1 backend APIs, Task 8 frontend page.
- 发布完成写回：Task 4 defines completion endpoint, Task 5 uses package data; actual card database write-back uses existing publish/write-back utilities and must be verified in Task 9.

### Placeholder Scan

The plan intentionally avoids placeholder markers and names concrete files, commands, and expected outcomes. Each task has a verification command and a commit boundary.

### Type Consistency

The visible user states are `正常`、`勘误`、`待发布`. Draft persisted statuses add `已归档` for completed historical records. Package statuses are `待发布`、`发布中`、`已发布`、`已退回`. Frontend types match backend response string values.
